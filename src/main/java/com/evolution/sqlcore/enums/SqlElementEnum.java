package com.evolution.sqlcore.enums;

public enum SqlElementEnum {
    SELECT,
    FROM,
    WHERE,
    ORDER_BY,
    GROUP_BY,
    HAVING,
    LIMIT,
    OFFSET,
    UNION,
    UNION_ALL,
    INTERSECT,
    EXCEPT,
    AND,
    OR,
    NOT,
    IN,
    BETWEEN,
    LIKE,
    IS_NULL,
    IS_NOT_NULL,
    EXISTS,
    CASE,
    WHEN,
    THEN,
    ELSE,
    END,
    AS,
    ASC,
    DESC,

    EQUAL,
    NOT_EQUAL,
    LESS_THAN,
    JOIN,
    LEFT_JOIN,
    RIGHT_JOIN,
    INNER_JOIN,
    OUTER_JOIN,
    FULL_JOIN,
    CROSS_JOIN,
    ON,
    USING,
    DISTINCT,
    VAL,
    TOP

}
